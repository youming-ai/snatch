# ===========================================
# Snatch - Environment Configuration Example
# ===========================================
# Copy this file to .env and modify the values
# cp .env.example .env

# ===========================================
# Frontend Configuration (Astro)
# ===========================================

# Rust API service URL (internal Docker network or external URL)
RUST_API_URL=http://localhost:3001

# Server binding
HOST=0.0.0.0
PORT=4321

# Node.js memory limit (for constrained environments)
NODE_OPTIONS=--max-old-space-size=256

# ===========================================
# Backend API Configuration (Rust)
# ===========================================

# API port (default: 3001)
# API_PORT=3001

# CORS allowed origins (comma-separated, leave empty for development mode)
# In production, set this to your domain(s)
# Example: ALLOWED_ORIGINS=https://snatch.example.com,https://www.snatch.example.com
ALLOWED_ORIGINS=

# Rust log level
RUST_LOG=info

# ===========================================
# Rate Limiting
# ===========================================

# Time window for rate limiting in milliseconds (default: 60000 = 1 minute)
RATE_LIMIT_WINDOW=60000

# Maximum requests per window (default: 10)
RATE_LIMIT_MAX=10

# ===========================================
# Security
# ===========================================

# Maximum URL length allowed (default: 2048)
MAX_URL_LENGTH=2048

# ===========================================
# Production Deployment
# ===========================================

# For Docker Compose deployment, these are typically set in docker-compose.yml
# For Vercel/Railway, set these in the platform's environment settings

# Example production setup:
# RUST_API_URL=https://api.snatch.example.com
# ALLOWED_ORIGINS=https://snatch.example.com
